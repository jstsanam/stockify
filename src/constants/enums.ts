export enum Status {
  LOADING = "loading",
  SUCCESS = "success",
  FAILED = "failed",
}

export enum TransactionStatus {
  PASSED = "Passed",
  FAILED = "Failed"
}

export enum TransactionType {
  BUY = "Buy",
  SELL = "Sell"
}

export enum GenderType {
  HE = "male",
  SHE = "female"
}